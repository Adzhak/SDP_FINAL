public class MagicDamageStrategy implements DamageStrategy {
    @Override
    public int calculateDamage(int baseDamage) {
        // Calculate magic damage (no additional calculations)
        return baseDamage;
    }
}